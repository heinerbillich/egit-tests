
test egit, eclipse and python ...

Global setup:

 Set up git
  git config --global user.name "Your Name"
  git config --global user.email heiner.billich@psi.ch
      

Next steps:

  mkdir egit-tests
  cd egit-tests
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:heinerbillich/egit-tests.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:heinerbillich/egit-tests.git
  git push -u origin master
      
